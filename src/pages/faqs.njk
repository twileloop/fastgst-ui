---
layout: base.njk
permalink: /faqs/
title: How to Find HSN & SAC Code & GST Rate – FAQs | FastGST.in
description: Step-by-step guide to check HSN codes, find GST rates, and validate HSN codes for products using FastGST API. Common questions about GST tax rate lookup.
keywords: HSN code lookup FAQ, GST rate finder guide, SAC code search help, FastGST FAQ, how to find GST rate
canonical: /faqs/
preloadFonts: true
schemaData: |
  <script type="application/ld+json">
  {
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
      "@id": "https://www.fastgst.in/faqs/#breadcrumb",
      "name": "Breadcrumbs",
      "itemListElement": [
          {
              "@type": "ListItem",
              "position": 1,
              "name": "Home",
              "item": "https://www.fastgst.in/"
          },
          {
              "@type": "ListItem",
              "position": 2,
              "name": "FAQs",
              "item": "https://www.fastgst.in/faqs/"
          }
      ]
  }
  </script>
customStyles: |
  .faq-item {
      border: 2px solid #e5e7eb;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  .faq-item:hover {
      border-color: #d1d5db;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  }

  .faq-item.active {
      border-color: #3b82f6;
      background-color: #ffffff;
  }

  .faq-button {
      position: relative;
      cursor: pointer;
      user-select: none;
  }

  .faq-button:active {
      transform: scale(0.99);
  }

  .faq-icon {
      transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      will-change: transform;
  }

  .faq-icon.rotated {
      transform: rotate(180deg);
  }

  .faq-content {
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .faq-content-inner {
      padding: 0 1.5rem;
      padding-bottom: 0;
      opacity: 0;
      transform: translateY(-10px);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .faq-content.active {
      max-height: 2000px;
  }

  .faq-content.active .faq-content-inner {
      padding-bottom: 1.5rem;
      opacity: 1;
      transform: translateY(0);
      transition-delay: 0.1s;
  }

  .faq-question {
      transition: color 0.2s ease;
  }

  .faq-item.active .faq-question {
      color: #3b82f6;
  }
customScripts: |
  <script>
      function toggleFAQ(button) {
          const faqItem = button.closest('.faq-item');
          const content = button.nextElementSibling;
          const icon = button.querySelector('.faq-icon');
          const isActive = faqItem.classList.contains('active');
          
          // Track FAQ interaction
          if (typeof gtag !== 'undefined') {
              gtag('event', 'faq_toggle', {
                  'event_category': 'FAQ',
                  'event_label': button.querySelector('h3').textContent,
                  'event_action': isActive ? 'close' : 'open'
              });
          }
          
          if (isActive) {
              // Close accordion
              faqItem.classList.remove('active');
              content.classList.remove('active');
              icon.classList.remove('rotated');
          } else {
              // Open accordion
              faqItem.classList.add('active');
              content.classList.add('active');
              icon.classList.add('rotated');
          }
      }

      // Close all accordions on page load for a clean start
      document.addEventListener('DOMContentLoaded', function() {
          document.querySelectorAll('.faq-item').forEach(item => {
              item.classList.remove('active');
          });
          document.querySelectorAll('.faq-content').forEach(content => {
              content.classList.remove('active');
          });
          document.querySelectorAll('.faq-icon').forEach(icon => {
              icon.classList.remove('rotated');
          });
      });
  </script>
---

<!-- Main Content -->
<main class="pt-16 overflow-x-hidden">
    <section class="py-20 bg-gray-50">
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
                <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                    Frequently Asked Questions
                </h1>
                <p class="text-lg text-gray-600 max-w-2xl mx-auto">
                    Common questions about our GST Tax Lookup API
                </p>
            </div>

            <!-- Still Have Questions -->
            <div class="bg-white rounded-2xl border-2 border-black p-8 md:p-12 text-center shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]">
                <h2 class="text-2xl font-bold text-gray-900 mb-4">Questions? We are here to help!</h2>
                <p class="text-gray-600 mb-8 max-w-lg mx-auto">
                    Our FAQ section is currently undergoing a complete update. If you need any assistance, have inquiries about tax lookup rates, or require support, please don't hesitate to reach out to us.
                </p>
                <a href="/contact/" class="inline-block bg-sky-200 text-black border-2 border-black px-8 py-3.5 rounded-lg font-extrabold shadow-[3px_3px_0px_0px_rgba(0,0,0,1)] hover:bg-sky-100 hover:shadow-[1px_1px_0px_0px_rgba(0,0,0,1)] hover:translate-x-[2px] hover:translate-y-[2px] active:translate-x-[3px] active:translate-y-[3px] active:shadow-none transform transition-all duration-100 uppercase tracking-wider text-sm">
                    Contact Us &rarr;
                </a>
            </div>
        </div>
    </section>
</main>

